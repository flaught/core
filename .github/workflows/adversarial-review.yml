# Flaught — Adversarial Review
#
# Dogfooding: runs Flaught against every PR to this repo.
# Uses Groq (llama-3.3-70b-versatile) for the LLM adversarial pass.
# The review comment is posted to the PR; the JSON artifact is uploaded
# for trend tracking. Exit code 1 (findings exceed severity gate) blocks merge.

name: Adversarial Review

on:
  pull_request:

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Flaught
        run: npm install -g @flaught/core

      - name: Install Semgrep
        run: pip install semgrep

      - name: Install project dependencies
        run: npm ci

      - name: Run adversarial review
        id: review
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          set +e
          PR_DESC="${{ github.event.pull_request.title }}"
          flaught review \
            --base "origin/${{ github.base_ref }}" \
            --head HEAD \
            --output findings.json \
            --pr-description "$PR_DESC" \
            --quiet
          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload findings artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flaught-findings
          path: findings.json
          retention-days: 90

      - name: Comment on PR
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Re-render the markdown report (no LLM cost, just deterministic findings)
          PR_DESC="${{ github.event.pull_request.title }}"
          BODY=$(flaught review \
            --base "origin/${{ github.base_ref }}" \
            --head HEAD \
            --no-llm \
            --pr-description "$PR_DESC" \
            --quiet 2>/dev/null || echo "Review could not be rendered.")

          if [ -f findings.json ]; then
            echo "$BODY" | gh pr comment ${{ github.event.pull_request.number }} --body-file -
          fi

      - name: Check severity gate
        if: steps.review.outputs.exit_code == '1'
        run: |
          echo "::error::Flaught found findings that exceed the severity gate threshold"
          exit 1

      - name: Check for errors
        if: steps.review.outputs.exit_code == '2'
        run: |
          echo "::error::Flaught encountered an error (config, API, or runtime)"
          exit 1